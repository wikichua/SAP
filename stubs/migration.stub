<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class Sap{%model%}Table extends Migration
{
    public function up()
    {
        cache()->forget('fillable-{%table_name%}');
        Schema::create('{%table_name%}', function (Blueprint $table) {
            $table->increments('id', true);
            {%migration_codes%}
            $table->integer('created_by')->nullable()->default(0);
            $table->integer('updated_by')->nullable()->default(0);
            $table->timestamps();
            $table->softDeletes();
            {%orderable_migration%}
        });

        {%settings_options_up%}
        app(config('sap.models.permission'))->createGroup('{%model_strings%}', ['Create {%model_strings%}', 'Read {%model_strings%}', 'Update {%model_strings%}', 'Delete {%model_strings%}']);
    }
    public function down()
    {
        {%settings_options_down%}
        app(config('sap.models.permission'))->where('group', '{%model_strings%}')->delete();
        Schema::dropIfExists('{%table_name%}');
    }
}
